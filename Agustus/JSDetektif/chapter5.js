// ==========================================
// FUNCTION 1 — Menghitung Suspicion Score
// ==========================================

function calculateSuspicionScore(person) {

    // Buat variabel untuk menyimpan total skor
    let totalScore = /* mulai dari 0 */;


    if (
        /* 1. Apakah person berada di TKP? */
    ) {
        // Tambahkan 40 poin
    }


    if (
        /* 2. Apakah person tidak memiliki alibi? */
    ) {
        // Tambahkan 35 poin
    }


    if (
        /* 3. Apakah person memiliki riwayat pelanggaran? */
    ) {
        // Tambahkan 25 poin
    }


    // Kembalikan total skor
}


// ==========================================
// FUNCTION 2 — Menentukan Status Tersangka
// ==========================================

function evaluateSuspectStatus(score) {

    if (
        /* 1. Apakah score >= 75? */
    ) {

        // Kembalikan "ARREST TARGET"

    } else {

        // Kembalikan "CLEAR"

    }
}


// ==========================================
// FUNCTION 3 — Menjalankan Investigation
// ==========================================

function runInvestigationSystem(suspectsList) {

    // Lakukan perulangan terhadap suspectsList

    for (/* tentukan cara iterasi */) {

        // Ambil satu data tersangka


        // Hitung suspicion score
        // Panggil Function 1


        // Tentukan status tersangka
        // Panggil Function 2


        // Tampilkan:
        // Nama tersangka
        // Total skor
        // Status
    }
}


// ==========================================
// JALANKAN SYSTEM
// ==========================================

// Panggil Function 3
// Gunakan suspectData sebagai argument