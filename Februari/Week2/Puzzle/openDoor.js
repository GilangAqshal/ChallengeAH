const hasTicket = false;
const isVIP = true;

// Gunakan operator logika yang tepat (OR / AND)
if (hasTicket || isVIP) {
    console.log("Silahkan masuk!");
} else {
    console.log("Maaf, tidak boleh masuk.");
}